# Installation

1. Create new virtual environment (i.e. venv)

2. Activate virtual environment (if not done automatically), i.e. for venv:

    `.venv/Scripts/activate`

2. Install dependencies:

    `pip install -r requirements.txt`

3. Execute test script:

    `python .\api_test_new.py`